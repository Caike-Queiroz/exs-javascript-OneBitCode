let nome = "Caike";

// "Print"
console.log(nome);

// Lançamento de um erro
console.error(nome);

// Aviso
console.warn(nome);

// Alerta
window.alert(nome);

// Confirmação
let confirmacao = window.confirm("Você aceita a utilização de cookies ?");

// Entrada
let sobrenome = window.prompt("Qual é o seu sobrenome ?");